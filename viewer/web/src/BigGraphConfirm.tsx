// Stub — replaced by Task H (canvas + BigGraphConfirm modal)
export interface BigGraphConfirmProps {
  file: { path: string; name: string; nodeCount: number };
  onCancel(): void;
  onConfirm(): void;
}

export function BigGraphConfirm(_props: BigGraphConfirmProps): null {
  return null;
}
